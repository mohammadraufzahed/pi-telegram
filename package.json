{
  "name": "pi-telegram",
  "version": "1.0.0",
  "description": "Pi coding agent extension: Telegram Bot API tools — send, react, pin, edit, journal history search.",
  "keywords": ["pi-package", "telegram", "pi-extension"],
  "license": "MIT",
  "repository": "mohammadraufzahed/pi-telegram",
  "pi": {
    "extensions": ["./extensions/index.ts"]
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "typebox": "*"
  },
  "scripts": {
    "check": "tsc --noEmit && node --test --import tsx tests/*.test.ts",
    "test": "npm run check"
  },
  "devDependencies": {
    "@types/node": "^22.7.5",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
